package com.qian.jianyin.playback

/**
 * Media3 Session 持有者（保留供未来使用）
 *
 * 注意：PlaybackSessionService 已废弃，统一使用 AudioPlaybackService。
 * AudioPlaybackService 内部使用 MediaSessionCompat，不依赖 Media3。
 *
 * 如果未来迁移到纯 Media3，可在此处实现相关逻辑。
 */
object Media3SessionHolder {
    // 预留，当前未激活
}
